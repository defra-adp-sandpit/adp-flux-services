apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: aj-demo-web
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:aj-demo-web-snd-02:tag"}
  values:
    environment: production
    name: aj-demo-web
    namespace: pp-demo
    image: sndadpinfcr2401.azurecr.io/image/aj-demo-web:0.1.0 # {"$imagepolicy": "flux-config:aj-demo-web-snd-02"}
    serviceAccount:
      name: aj-demo-web
    labels:
      component: web
    container:
      imagePullPolicy: Always
    ingress:
      class: nginx
      endpoint: __INGRESS_ENDPOINT__
      server: snd2.adp.defra.gov.uk
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
