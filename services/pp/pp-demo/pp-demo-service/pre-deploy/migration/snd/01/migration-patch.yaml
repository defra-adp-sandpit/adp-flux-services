apiVersion: batch/v1
kind: Job
metadata:
  name: pp-demo-service-dbmigration
  namespace: pp-demo
spec:
  template:
    spec:
      containers:
      - name: pp-demo-service-dbmigration
        image: ssvadpinfcr3401.azurecr.io/image/pp-demo-service-dbmigration:0.1.0 # {"$imagepolicy": "flux-config:pp-demo-service-dbmigration-snd-01"}
        env:
        - name: POSTGRES_HOST
          value: ${SHARED_POSTGRES_SERVER_01}.postgres.database.azure.com
        - name: SCHEMA_NAME
          value: ${POSTGRES_SCHEMA_NAME}
        - name: POSTGRES_DB
          value: ${POSTGRES_DB}
        - name: POSTGRES_PORT
          value: ${POSTGRES_PORT}
        - name: SCHEMA_USERNAME
          value: AAG-Azure-ADP-pp-snd1-PostgresDB_Writer
