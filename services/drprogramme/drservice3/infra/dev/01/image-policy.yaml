apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: drservice3-dev-01
  namespace: flux-config
spec:
  imageRepositoryRef:
    name: drservice3
  policy:
    semver:
      range: '>=1.0.0'
