apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: drservice2
  namespace: flux-config
spec:
  chart:
    spec:
      version: "4.32.10" # {"$imagepolicy": "flux-config:drservice2-snd-01:tag"}
  values:
    environment: production
    name: drservice2
    namespace: drprogramme
    image: sndadpinfcr1401.azurecr.io/image/drservice2:4.32.10 # {"$imagepolicy": "flux-config:drservice2-snd-01"}
    serviceAccount:
      name: drservice2
    labels:
      component: web
    container:
      imagePullPolicy: Always
      requestMemory: 50Mi
      requestCpu: 50m
      limitMemory: 200Mi
      limitCpu: 400m
    ingress:
      class: nginx
      endpoint: drservice2
      server: snd1.adp.defra.gov.uk
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
