apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: poc-snd-02
  namespace: flux-config
spec:
  imageRepositoryRef:
    name: poc
  policy:
    semver:
      range: '>=1.0.0'
