apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-helm-only-9
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: test-helm-only-9
    backstage.io/kubernetes-team: ffc-demo10
spec:
  releaseName: test-helm-only-9
  chart:
    spec:
      chart: test-helm-only-9
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: ffc-demo10-helm-repo
        namespace: flux-config
  install:
    createNamespace: "true"
  interval: 5m0s
  targetNamespace: ffc-demo10
