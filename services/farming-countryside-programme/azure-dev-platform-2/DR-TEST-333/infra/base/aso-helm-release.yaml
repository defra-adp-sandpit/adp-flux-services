apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: DR-TEST-333-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: DR-TEST-333
    backstage.io/kubernetes-team: azure-dev-platform-2
spec:
  releaseName: DR-TEST-333-infra
  chart:
    spec:
      chart: DR-TEST-333-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: azure-dev-platform-2-helm-repo
        namespace: flux-config
  install:
    createNamespace: "true"
  interval: 5m0s
  targetNamespace: azure-dev-platform-2
  values:
    namespace: azure-dev-platform-2
