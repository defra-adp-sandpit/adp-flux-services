apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-team-sync-1
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:test-team-sync-1-snd-03:tag"}
  values:
    environment: production
    name: test-team-sync-1
    namespace: crai-james-test-project
    image: sndadpinfcr3401.azurecr.io/image/test-team-sync-1:0.1.0 # {"$imagepolicy": "flux-config:test-team-sync-1-snd-03"}
    serviceAccount:
      name: test-team-sync-1
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
