apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: test-team-sync-1
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: test-team-sync-1
    backstage.io/kubernetes-team: crai-james-test-project
spec:
  image: ${APPLICATION_ACR_NAME}.azurecr.io/image/test-team-sync-1
  interval: 5m0s
  provider: azure
