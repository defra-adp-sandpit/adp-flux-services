apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: coreai-mcu-responses
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:coreai-mcu-responses-snd-01:tag"}
  values:
    environment: production
    name: coreai-mcu-responses
    namespace: crai-mcu
    image: sndadpinfcr1401.azurecr.io/image/coreai-mcu-responses:0.1.0 # {"$imagepolicy": "flux-config:coreai-mcu-responses-snd-01"}
    serviceAccount:
      name: coreai-mcu-responses
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
