apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: aaservice1-dev-01
  namespace: flux-config
spec:
  imageRepositoryRef:
    name: aaservice1
  policy:
    semver:
      range: '>=1.0.0'
