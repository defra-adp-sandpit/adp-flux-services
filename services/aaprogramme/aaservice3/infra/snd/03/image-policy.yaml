apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: aaservice3-snd-03
  namespace: flux-config
spec:
  imageRepositoryRef:
    name: aaservice3
  policy:
    semver:
      range: '>=1.0.0'
