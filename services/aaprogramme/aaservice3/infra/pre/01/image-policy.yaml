apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: aaservice3-pre-01
  namespace: flux-config
spec:
  imageRepositoryRef:
    name: aaservice3
  policy:
    semver:
      range: '>=1.0.0'
