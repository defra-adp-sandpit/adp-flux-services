apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: aaservice3
  namespace: flux-config
spec:
  chart:
    spec:
      version: "4.32.10" # {"$imagepolicy": "flux-config:aaservice3-snd-02:tag"}
  values:
    environment: production
    name: aaservice3
    namespace: aaprogramme
    image: sndadpinfcr1402.azurecr.io/image/aaservice3:4.32.10 # {"$imagepolicy": "flux-config:aaservice3-snd-02"}
    serviceAccount:
      name: aaservice3
    labels:
      component: web
    container:
      imagePullPolicy: Always
      requestMemory: 50Mi
      requestCpu: 50m
      limitMemory: 200Mi
      limitCpu: 400m
    ingress:
      class: nginx
      endpoint: aaservice3
      server: snd1.adp.defra.gov.uk
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
