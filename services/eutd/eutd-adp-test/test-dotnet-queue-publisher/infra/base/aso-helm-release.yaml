apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-dotnet-queue-publisher-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: test-dotnet-queue-publisher
    backstage.io/kubernetes-team: eutd-adp-test
spec:
  releaseName: test-dotnet-queue-publisher-infra
  chart:
    spec:
      chart: test-dotnet-queue-publisher-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: eutd-adp-test-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: eutd-adp-test
  values:
    namespace: eutd-adp-test
