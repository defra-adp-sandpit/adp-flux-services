apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-dotnet-topic-subscriber
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:test-dotnet-topic-subscriber-snd-03:tag"}
  values:
    environment: production
    name: test-dotnet-topic-subscriber
    namespace: eutd-adp-test
    image: sndadpinfcr3401.azurecr.io/image/test-dotnet-topic-subscriber:0.1.0 # {"$imagepolicy": "flux-config:test-dotnet-topic-subscriber-snd-03"}
    serviceAccount:
      name: test-dotnet-topic-subscriber
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
