apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-node-backend-jkr
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:test-node-backend-jkr-snd-03:tag"}
  values:
    environment: production
    name: test-node-backend-jkr
    namespace: eutd-adp-test
    image: sndadpinfcr3401.azurecr.io/image/test-node-backend-jkr:0.1.0 # {"$imagepolicy": "flux-config:test-node-backend-jkr-snd-03"}
    serviceAccount:
      name: test-node-backend-jkr
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
