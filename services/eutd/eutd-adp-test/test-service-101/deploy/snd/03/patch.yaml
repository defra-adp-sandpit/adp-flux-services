apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-service-101
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:test-service-101-snd-03:tag"}
  values:
    environment: production
    name: test-service-101
    namespace: eutd-adp-test
    image: sndadpinfcr3401.azurecr.io/image/test-service-101:0.1.0 # {"$imagepolicy": "flux-config:test-service-101-snd-03"}
    serviceAccount:
      name: test-service-101
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
