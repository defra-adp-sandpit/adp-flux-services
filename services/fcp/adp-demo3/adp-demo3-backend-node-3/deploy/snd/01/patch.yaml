apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adp-demo3-backend-node-3
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.1-alpha.611080 # {"$imagepolicy": "flux-config:adp-demo3-backend-node-3-snd-01:tag"}
  values:
    environment: production
    name: adp-demo3-backend-node-3
    namespace: adp-demo3
    teamResourceGroupName: ${TEAM_RG}
    image: sndadpinfcr1401.azurecr.io/image/adp-demo3-backend-node-3:0.1.1-alpha.611080 # {"$imagepolicy": "flux-config:adp-demo3-backend-node-3-snd-01"}
    serviceAccount:
      name: adp-demo3-backend-node-3
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
    storageAccountPrefix: ${STORAGEACCOUNT_PREFIX}
    enablePrivateEndpoint: ${ENABLE_PRIVATE_ENDPOINT}
