apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo16-web-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo16-web
    backstage.io/kubernetes-team: fcp-demo16
spec:
  releaseName: ffc-demo16-web-infra
  chart:
    spec:
      chart: ffc-demo16-web-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo16-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo16
  values:
    namespace: fcp-demo16
