apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo30-payment-web
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo30-payment-web
    backstage.io/kubernetes-team: fcp-demo30
spec:
  releaseName: ffc-demo30-payment-web
  chart:
    spec:
      chart: ffc-demo30-payment-web
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo30-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo30
