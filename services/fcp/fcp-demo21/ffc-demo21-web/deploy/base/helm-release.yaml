apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo21-web
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo21-web
    backstage.io/kubernetes-team: fcp-demo21
spec:
  releaseName: ffc-demo21-web
  chart:
    spec:
      chart: ffc-demo-web
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo21-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo21
