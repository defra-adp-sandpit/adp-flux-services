apiVersion: batch/v1
kind: Job
metadata:
  name: ffc-demo10-claim-service-dbmigration
  namespace: fcp-demo10
  labels:
    azure.workload.identity/use: "true"
    backstage.io/kubernetes-id: ffc-demo10-claim-service
    backstage.io/kubernetes-team: fcp-demo10
spec:
  template:
    metadata:
      labels:
        azure.workload.identity/use: "true"
        backstage.io/kubernetes-id: ffc-demo10-claim-service
        backstage.io/kubernetes-team: fcp-demo10
    spec:
      serviceAccountName: ${TEAM_NAMESPACE}
      restartPolicy: Never
      containers:
      - name: ffc-demo10-claim-service-dbmigration
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 100m
            memory: 250Mi
          limits:
            cpu: 500m
            memory: 600Mi
  backoffLimit: 2
