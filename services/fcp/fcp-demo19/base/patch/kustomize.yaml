apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: fcp-demo19-base
  namespace: flux-config
  labels:
    backstage.io/kubernetes-team: fcp-demo19
spec:
  sourceRef:
    kind: GitRepository
    name: services-repository
    namespace: flux-config
  interval: 5m
  timeout: 3m
  prune: true
  path: ./services/fcp/fcp-demo19/${ENVIRONMENT}/${ENVIRONMENT_ID}
  postBuild:
    substitute:
      TEAM_NAMESPACE: fcp-demo19
      TEAM_NAME: fcp-demo19-team
      TEAM_RG: ${SERVICES_INFRA_RG}-fcp-demo19
      APPCONFIG_MI_CLIENTID: ${APPCONFIG_MI_CLIENTID}
      APPCONFIG_NAME: ${APPCONFIG_NAME}
      SERVICE_CODE: fcp-demo19
      PLATFORM_DB_ADMIN: adp-platform-db-aad-admin
      TEAM_CPU_QUOTA: "5000"
      TEAM_MEMORY_QUOTA: 5000Mi
      TEAM_PODS_QUOTA: "20"
      CONTAINER_MAX_CPU: 600m
      CONTAINER_MAX_MEMORY: 1000Mi
    substituteFrom:
    - kind: ConfigMap
      name: fcp-demo19-mi-credential
      optional: true
    - kind: ConfigMap
      name: adp-platform-vars
