apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo18-calculation-service-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo18-calculation-service
    backstage.io/kubernetes-team: fcp-demo18
spec:
  releaseName: ffc-demo18-calculation-service-infra
  chart:
    spec:
      chart: ffc-demo18-calculation-service-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo18-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo18
  values:
    namespace: fcp-demo18
