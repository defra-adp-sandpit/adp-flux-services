apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adp-demo10-backend-node-5
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:adp-demo10-backend-node-5-snd-01:tag"}
  values:
    environment: production
    name: adp-demo10-backend-node-5
    namespace: adp-demo10
    teamResourceGroupName: ${TEAM_RG}
    image: sndadpinfcr1401.azurecr.io/image/adp-demo10-backend-node-5:0.1.0 # {"$imagepolicy": "flux-config:adp-demo10-backend-node-5-snd-01"}
    serviceAccount:
      name: adp-demo10-backend-node-5
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
    storageAccountPrefix: ${STORAGEACCOUNT_PREFIX}
    enablePrivateEndpoint: ${ENABLE_PRIVATE_ENDPOINT}
