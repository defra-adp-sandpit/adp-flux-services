apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo32-claim-service
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo32-claim-service
    backstage.io/kubernetes-team: fcp-demo32
spec:
  releaseName: ffc-demo32-claim-service
  chart:
    spec:
      chart: ffc-demo-claim-service
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo32-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo32
