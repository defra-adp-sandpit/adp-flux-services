apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo38-payment-web
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo38-payment-web
    backstage.io/kubernetes-team: fcp-demo38
spec:
  releaseName: ffc-demo38-payment-web
  chart:
    spec:
      chart: ffc-demo-payment-web
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo38-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo38
