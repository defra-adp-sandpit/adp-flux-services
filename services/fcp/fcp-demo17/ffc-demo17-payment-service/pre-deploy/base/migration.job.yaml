apiVersion: batch/v1
kind: Job
metadata:
  name: ffc-demo17-payment-service-dbmigration
  namespace: fcp-demo17
  labels:
    azure.workload.identity/use: "true"
    backstage.io/kubernetes-id: ffc-demo17-payment-service
    backstage.io/kubernetes-team: fcp-demo17
spec:
  template:
    metadata:
      labels:
        azure.workload.identity/use: "true"
        backstage.io/kubernetes-id: ffc-demo17-payment-service
        backstage.io/kubernetes-team: fcp-demo17
    spec:
      serviceAccountName: ${TEAM_NAMESPACE}
      restartPolicy: Never
      containers:
      - name: ffc-demo17-payment-service-dbmigration
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 100m
            memory: 250Mi
          limits:
            cpu: 500m
            memory: 600Mi
  backoffLimit: 2
