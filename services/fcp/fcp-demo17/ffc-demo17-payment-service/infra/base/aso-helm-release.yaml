apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo17-payment-service-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo17-payment-service
    backstage.io/kubernetes-team: fcp-demo17
spec:
  releaseName: ffc-demo17-payment-service-infra
  chart:
    spec:
      chart: ffc-demo-payment-service-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo17-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo17
  values:
    namespace: fcp-demo17
