apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo14-web
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:ffc-demo14-web-snd-01:tag"}
  values:
    environment: production
    name: ffc-demo14-web
    namespace: fcp-demo14
    teamResourceGroupName: ${TEAM_RG}
    image: sndadpinfcr1401.azurecr.io/image/ffc-demo14-web:0.1.0 # {"$imagepolicy": "flux-config:ffc-demo14-web-snd-01"}
    serviceAccount:
      name: ffc-demo14-web
    labels:
      component: web
    container:
      imagePullPolicy: Always
    ingress:
      class: nginx
      endpoint: ffc-demo14-web
      server: snd1.adp.defra.gov.uk
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
    storageAccountPrefix: ${STORAGEACCOUNT_PREFIX}
    enablePrivateEndpoint: ${ENABLE_PRIVATE_ENDPOINT}
