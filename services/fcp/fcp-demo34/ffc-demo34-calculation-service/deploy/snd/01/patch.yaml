apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo34-calculation-service
  namespace: flux-config
spec:
  chart:
    spec:
      version: 4.3.29-alpha.554809 # {"$imagepolicy": "flux-config:ffc-demo-calculation-service-snd-01:tag"}
  values:
    environment: production
    name: ffc-demo34-calculation-service
    namespace: fcp-demo34
    teamResourceGroupName: ${TEAM_RG}
    image: sndadpinfcr1401.azurecr.io/image/ffc-demo-calculation-service:4.3.29-alpha.554809 # {"$imagepolicy": "flux-config:ffc-demo-calculation-service-snd-01"}
    serviceAccount:
      name: ffc-demo34-calculation-service
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
    storageAccountPrefix: ${STORAGEACCOUNT_PREFIX}
    enablePrivateEndpoint: ${ENABLE_PRIVATE_ENDPOINT}
