apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo34-web
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo34-web
    backstage.io/kubernetes-team: fcp-demo34
spec:
  releaseName: ffc-demo34-web
  chart:
    spec:
      chart: ffc-demo34-web
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo34-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo34
