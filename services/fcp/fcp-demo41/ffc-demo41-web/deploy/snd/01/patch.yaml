apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo41-web
  namespace: flux-config
spec:
  chart:
    spec:
      version: 4.33.4-alpha.607389 # {"$imagepolicy": "flux-config:ffc-demo-web-snd-01:tag"}
  values:
    environment: production
    name: ffc-demo41-web
    namespace: fcp-demo41
    teamResourceGroupName: ${TEAM_RG}
    image: sndadpinfcr1401.azurecr.io/image/ffc-demo-web:4.33.4-alpha.607389 # {"$imagepolicy": "flux-config:ffc-demo-web-snd-01"}
    serviceAccount:
      name: ffc-demo41-web
    labels:
      component: web
    container:
      imagePullPolicy: Always
    ingress:
      class: nginx
      endpoint: ffc-demo41-web
      server: snd1.adp.defra.gov.uk
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
    storageAccountPrefix: ${STORAGEACCOUNT_PREFIX}
    enablePrivateEndpoint: ${ENABLE_PRIVATE_ENDPOINT}
