apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo23-web
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo23-web
    backstage.io/kubernetes-team: fcp-demo23
spec:
  releaseName: ffc-demo23-web
  chart:
    spec:
      chart: ffc-demo-web
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo23-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo23
