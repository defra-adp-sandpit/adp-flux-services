apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo6-payment-web
  namespace: flux-config
spec:
  chart:
    spec:
      version: 1.20.5-alpha.572929 # {"$imagepolicy": "flux-config:ffc-demo-payment-web-snd-01:tag"}
  values:
    environment: production
    name: ffc-demo6-payment-web
    namespace: fcp-demo6
    teamResourceGroupName: ${TEAM_RG}
    image: sndadpinfcr1401.azurecr.io/image/ffc-demo-payment-web:1.20.5-alpha.572929 # {"$imagepolicy": "flux-config:ffc-demo-payment-web-snd-01"}
    serviceAccount:
      name: ffc-demo6-payment-web
    labels:
      component: web
    container:
      imagePullPolicy: Always
    ingress:
      class: nginx
      endpoint: ffc-demo6-payment-web
      server: snd1.adp.defra.gov.uk
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
    storageAccountPrefix: ${STORAGEACCOUNT_PREFIX}
    enablePrivateEndpoint: ${ENABLE_PRIVATE_ENDPOINT}
