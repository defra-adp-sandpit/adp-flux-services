apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo6-payment-web-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo6-payment-web
    backstage.io/kubernetes-team: fcp-demo6
spec:
  releaseName: ffc-demo6-payment-web-infra
  chart:
    spec:
      chart: ffc-demo6-payment-web-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo6-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo6
  values:
    namespace: fcp-demo6
