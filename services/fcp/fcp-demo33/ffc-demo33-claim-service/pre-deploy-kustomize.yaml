apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ffc-demo33-claim-service-pre-deploy
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo33-claim-service
    backstage.io/kubernetes-team: fcp-demo33
spec:
  dependsOn:
  - name: ffc-demo33-claim-service-infra
    namespace: flux-config
  sourceRef:
    kind: GitRepository
    name: services-repository
    namespace: flux-config
  path: ./services/fcp/fcp-demo33/ffc-demo33-claim-service/pre-deploy/${ENVIRONMENT}/${ENVIRONMENT_ID}
  interval: 60m0s
  timeout: 3m0s 
  prune: true
  wait: true
  force: true
  retryInterval: 2m0s
  postBuild:
    substitute:
      SHARED_POSTGRES_SERVER_01: ${SHARED_POSTGRES_SERVER_01}
      SERVICE_NAME: ffc-demo33-claim-service
      TEAM_NAMESPACE: ${TEAM_NAMESPACE}
      TEAM_NAME: ${TEAM_NAME}
      PLATFORM_DB_ADMIN: ${PLATFORM_DB_ADMIN}
      POSTGRES_DB_NAME: fcp-demo-claim
      POSTGRES_PORT: '"5432"'
      POSTGRES_SCHEMA_NAME: public
    substituteFrom:
    - kind: ConfigMap
      name: adp-platform-vars
    - kind: ConfigMap
      name: ffc-demo33-claim-service-mi-credential
