apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-backend-dotnet-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: test-backend-dotnet
    backstage.io/kubernetes-team: fcp-jkr-test
spec:
  releaseName: test-backend-dotnet-infra
  chart:
    spec:
      chart: test-backend-dotnet-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-jkr-test-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-jkr-test
  values:
    namespace: fcp-jkr-test
