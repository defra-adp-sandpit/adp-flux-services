apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-svc-1-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: test-svc-1
    backstage.io/kubernetes-team: fcp-jkr-test
spec:
  releaseName: test-svc-1-infra
  chart:
    spec:
      chart: test-svc-1-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-jkr-test-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-jkr-test
  values:
    namespace: fcp-jkr-test
