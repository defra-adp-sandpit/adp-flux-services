apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo2-payment-service
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo2-payment-service
    backstage.io/kubernetes-team: fcp-demo2
spec:
  releaseName: ffc-demo2-payment-service
  chart:
    spec:
      chart: ffc-demo-payment-service
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo2-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo2
