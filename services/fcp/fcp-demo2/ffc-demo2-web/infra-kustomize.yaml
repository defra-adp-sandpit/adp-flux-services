apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ffc-demo2-web-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo2-web
    backstage.io/kubernetes-team: fcp-demo2
spec:
  sourceRef:
    kind: GitRepository
    name: services-repository
    namespace: flux-config
  path: ./services/fcp/fcp-demo2/ffc-demo2-web/infra/${ENVIRONMENT}/${ENVIRONMENT_ID}
  interval: 60m0s
  timeout: 3m0s 
  prune: true
  wait: true
  force: true
  retryInterval: 2m0s
  postBuild:
    substitute:
      SERVICE_NAME: ffc-demo2-web
      TEAM_RG: ${TEAM_RG}
      TEAM_NAMESPACE: ${TEAM_NAMESPACE}
      TEAM_NAME: ${TEAM_NAME}
      SERVICE_CODE: ${SERVICE_CODE}
    substituteFrom:
    - kind: ConfigMap
      name: adp-platform-vars
