apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adp-demo5-backend-node-4
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.1-alpha.581980 # {"$imagepolicy": "flux-config:adp-demo5-backend-node-4-snd-01:tag"}
  values:
    environment: production
    name: adp-demo5-backend-node-4
    namespace: adp-demo5
    teamResourceGroupName: ${TEAM_RG}
    image: sndadpinfcr1401.azurecr.io/image/fcp-jkr-demo-backend-node-1:0.1.1-alpha.581980 # {"$imagepolicy": "flux-config:adp-demo5-backend-node-4-snd-01"}
    serviceAccount:
      name: adp-demo5-backend-node-4
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
    storageAccountPrefix: ${STORAGEACCOUNT_PREFIX}
    enablePrivateEndpoint: ${ENABLE_PRIVATE_ENDPOINT}
