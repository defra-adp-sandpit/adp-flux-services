apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo4-payment-web-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo4-payment-web
    backstage.io/kubernetes-team: fcp-demo4
spec:
  releaseName: ffc-demo4-payment-web-infra
  chart:
    spec:
      chart: ffc-demo-payment-web-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo4-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo4
  values:
    namespace: fcp-demo4
