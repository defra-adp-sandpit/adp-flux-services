apiVersion: batch/v1
kind: Job
metadata:
  name: ffc-demo4-claim-service-dbmigration
  namespace: fcp-demo4
  labels:
    azure.workload.identity/use: "true"
    backstage.io/kubernetes-id: ffc-demo4-claim-service
    backstage.io/kubernetes-team: fcp-demo4
spec:
  template:
    metadata:
      labels:
        azure.workload.identity/use: "true"
        backstage.io/kubernetes-id: ffc-demo4-claim-service
        backstage.io/kubernetes-team: fcp-demo4
    spec:
      serviceAccountName: ${TEAM_NAMESPACE}
      restartPolicy: Never
      containers:
      - name: ffc-demo4-claim-service-dbmigration
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 100m
            memory: 250Mi
          limits:
            cpu: 500m
            memory: 600Mi
  backoffLimit: 2
