apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo8-calculation-service
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo8-calculation-service
    backstage.io/kubernetes-team: fcp-demo8
spec:
  releaseName: ffc-demo8-calculation-service
  chart:
    spec:
      chart: ffc-demo8-calculation-service
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo8-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo8
