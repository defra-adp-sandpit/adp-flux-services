apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo3-payment-service
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo3-payment-service
    backstage.io/kubernetes-team: fcp-demo3
spec:
  releaseName: ffc-demo3-payment-service
  chart:
    spec:
      chart: ffc-demo3-payment-service
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo3-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo3
