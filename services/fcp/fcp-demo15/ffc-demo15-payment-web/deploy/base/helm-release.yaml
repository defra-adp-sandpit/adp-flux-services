apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo15-payment-web
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo15-payment-web
    backstage.io/kubernetes-team: fcp-demo15
spec:
  releaseName: ffc-demo15-payment-web
  chart:
    spec:
      chart: ffc-demo-payment-web
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo15-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo15
