apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ncea-geonetwork
  namespace: flux-config
spec:
  chart:
    spec:
      version: 4.2.8-alpha.601311 # {"$imagepolicy": "flux-config:ncea-geonetwork-snd-01:tag"}
  values:
    environment: production
    name: ncea-geonetwork
    namespace: ncea-search
    teamResourceGroupName: ${TEAM_RG}
    image: sndadpinfcr1401.azurecr.io/image/ncea-geonetwork:4.2.8-alpha.601311 # {"$imagepolicy": "flux-config:ncea-geonetwork-snd-01"}
    serviceAccount:
      name: ncea-geonetwork
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
    storageAccountPrefix: ${STORAGEACCOUNT_PREFIX}
    enablePrivateEndpoint: ${ENABLE_PRIVATE_ENDPOINT}
