apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-ingress-5
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:test-ingress-5-snd-02:tag"}
  values:
    environment: production
    name: test-ingress-5
    namespace: test-demo
    teamResourceGroupName: ${TEAM_RG}
    image: sndadpinfcr2401.azurecr.io/image/test-ingress-5:0.1.0 # {"$imagepolicy": "flux-config:test-ingress-5-snd-02"}
    serviceAccount:
      name: test-ingress-5
    labels:
      component: web
    container:
      imagePullPolicy: Always
    ingress:
      class: nginx
      endpoint: __INGRESS_ENDPOINT__
      server: snd2.adp.defra.gov.uk
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
    storageAccountPrefix: ${STORAGEACCOUNT_PREFIX}
    enablePrivateEndpoint: ${ENABLE_PRIVATE_ENDPOINT}
