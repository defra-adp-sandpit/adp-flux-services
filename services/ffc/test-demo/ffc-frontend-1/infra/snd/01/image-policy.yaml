apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ffc-frontend-1-snd-01
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-frontend-1
    backstage.io/kubernetes-team: test-demo
spec:
  imageRepositoryRef:
    name: ffc-frontend-1
  policy:
    semver:
      range: '>=0.1.0'
