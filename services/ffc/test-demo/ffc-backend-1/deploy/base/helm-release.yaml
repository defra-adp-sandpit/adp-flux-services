apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-backend-1
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-backend-1
    backstage.io/kubernetes-team: test-demo
spec:
  releaseName: ffc-backend-1
  chart:
    spec:
      chart: ffc-backend-1
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: test-demo-helm-repo
        namespace: flux-config
  install:
    createNamespace: "true"
  interval: 5m0s
  targetNamespace: test-demo
