apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-ingress-4
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: test-ingress-4
    backstage.io/kubernetes-team: test-demo
spec:
  releaseName: test-ingress-4
  chart:
    spec:
      chart: test-ingress-4
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: test-demo-helm-repo
        namespace: flux-config
  install:
    createNamespace: "true"
  interval: 5m0s
  targetNamespace: test-demo
