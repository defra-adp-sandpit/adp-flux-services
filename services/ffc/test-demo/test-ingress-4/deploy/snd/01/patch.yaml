apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-ingress-4
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:test-ingress-4-snd-01:tag"}
  values:
    environment: production
    name: test-ingress-4
    namespace: test-demo
    image: sndadpinfcr1401.azurecr.io/image/test-ingress-4:0.1.0 # {"$imagepolicy": "flux-config:test-ingress-4-snd-01"}
    serviceAccount:
      name: test-ingress-4
    labels:
      component: web
    container:
      imagePullPolicy: Always
    ingress:
      class: nginx
      endpoint: __INGRESS_ENDPOINT__
      server: snd1.adp.defra.gov.uk
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
