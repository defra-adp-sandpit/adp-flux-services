apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: jc-mystery-frontend-1
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: jc-mystery-frontend-1
    backstage.io/kubernetes-team: fcp-demo
spec:
  releaseName: jc-mystery-frontend-1
  chart:
    spec:
      chart: jc-mystery-frontend-1
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo
