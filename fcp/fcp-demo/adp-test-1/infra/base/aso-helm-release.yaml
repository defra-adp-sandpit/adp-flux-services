apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adp-test-1-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: adp-test-1
    backstage.io/kubernetes-team: fcp-demo
spec:
  releaseName: adp-test-1-infra
  chart:
    spec:
      chart: adp-test-1-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo
  values:
    namespace: fcp-demo
