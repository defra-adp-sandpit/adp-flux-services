apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adp-test-dr555
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: adp-test-dr555
    backstage.io/kubernetes-team: azure-dev-platform
spec:
  releaseName: adp-test-dr555
  chart:
    spec:
      chart: adp-test-dr555
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: azure-dev-platform-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: azure-dev-platform
