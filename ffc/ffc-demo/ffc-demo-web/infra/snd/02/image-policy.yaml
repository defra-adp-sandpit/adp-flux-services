apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ffc-demo-web-__ENVIRONMENT__-0__ENV_INSTANCE__
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ffc-demo-web
    backstage.io/kubernetes-team: ffc-demo
spec:
  imageRepositoryRef:
    name: ffc-demo-web
  policy:
    semver:
      range: '>=0.1.0'
